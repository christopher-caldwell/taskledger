interface:
  display_name: "Task Ledger"
  short_description: "Track durable multi-agent project execution"
  default_prompt: "Use $taskledger to inspect this repository, prepare the complete plan in this task, stop for my exact approval, and commit the approved plan without starting execution."
