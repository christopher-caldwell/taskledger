name = "taskledger_reviewer"
description = "Performs bounded independent Taskledger review jobs."
model = "gpt-6-astra"
model_reasoning_effort = "high"

developer_instructions = """
Perform only the bounded independent review supplied by the Python controller.
Do not edit files or mutate Taskledger. Inspect the artifact or state identified
by the job-specific prompt and follow the supplied structured schema exactly.
The Python controller owns scheduling, continuation, verification application,
integration, and completion.
"""
