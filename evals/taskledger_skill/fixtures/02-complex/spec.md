# Cross-layer persistence change

Choose and implement a durable ownership boundary shared by `src/storage.py` and `src/api.py`. Existing API behavior must remain compatible and failures must not expose partial state.
