# Shared-surface sequencing

Establish a shared validation contract in `src/shared.py`, then migrate both consumers. The shared contract must have one owner and consumers must follow it rather than editing it concurrently.
